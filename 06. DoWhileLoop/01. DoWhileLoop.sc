// scala program for do while loop

// same as while but it must run once for do block

// creating object
object MainObject {  
	def main(args: Array[String]) {
		// initialization 
        var a = 10;
        do {
            println( a );
            // increment 
            a = a + 2; 
        }
        // condition
        while( a <= 20 )  
   }  
}  